[pytest]
markers =
    performance: Performance and load tests (deselect with '-m "not performance"')
    slow: Slow-running tests
    concurrency: Tests for concurrent updates and race conditions
    retry: Tests for retry logic and backoff timing
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
